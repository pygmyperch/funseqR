% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubmed_enrichment.R
\name{search_pubmed_enhanced}
\alias{search_pubmed_enhanced}
\title{PubMed Literature Search for Enriched Genes}
\usage{
search_pubmed_enhanced(
  uniprot_accession,
  gene_name = "",
  entry_name = "",
  species = "danio rerio",
  max_results = 5,
  custom_search_term = NULL,
  include_functional_mapping = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{uniprot_accession}{Character. UniProt accession number}

\item{gene_name}{Character. Gene name or symbol (can be empty)}

\item{entry_name}{Character. UniProt entry name}

\item{species}{Character. Species name (default "danio rerio")}

\item{max_results}{Integer. Maximum number of papers to retrieve (default 5)}

\item{custom_search_term}{Character. Optional custom search term to include}

\item{include_functional_mapping}{Logical. Use known functional mappings (default TRUE)}

\item{verbose}{Logical. Print progress information (default TRUE)}
}
\value{
Data frame with PubMed search results
}
\description{
Functions to search PubMed for literature related to genes associated with
enriched GO terms and generate reference lists.
Enhanced PubMed search for genes with multiple strategies
}
\details{
This function uses multiple search strategies to find relevant literature:
\enumerate{
\item Gene names (if available and suitable length)
\item UniProt accessions
\item Functional names derived from entry names
\item Custom search terms provided by user
\item Known functional mappings (lectin, chymotrypsinogen, etc.)
}

Each search strategy can find up to max_results papers. The function tries
different strategies sequentially and stops early if sufficient papers are found.
For each search term, multiple query variations are attempted (species-specific,
broad taxonomic, functional combinations).
}
